apiVersion: v1
kind: Service
metadata:
  name: {{ include "circleci.fullname" . }}
  labels:
    {{- include "circleci.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "circleci.selectorLabels" . | nindent 4 }}

--- 

apiVersion: v1
kind: Service
metadata:
  name: {{ include "circleci.fullname" . }}-ui
  labels:
    app: {{ include "circleci.fullname" . }}
    #tier: frontend
    #chart: {{ include "circleci.chart" . }}
    #release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    protocol: TCP
    targetPort: 80
    name: http
  selector:
    app: {{ include "circleci.fullname" . }}
    #tier: frontend
    #release: {{ .Release.Name }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "circleci.fullname" . }}-redis-server
  labels:
    app: {{ include "circleci.fullname" . }}
    #tier: cache
    #chart: {{ include "circleci.chart" . }}
    #release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: ClusterIP
  ports:
  - port: 6379
  selector:
    app: {{ include "circleci.fullname" . }}
    #tier: cache
    #release: {{ .Release.Name }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "circleci.fullname" . }}-db
  labels:
    app: {{ include "circleci.fullname" . }}
    #tier: backenddb
    #release: {{ .Release.Name }}
    #chart: {{ include "circleci.chart" . }}
spec:
  type: ClusterIP
  ports:
  - port: 5432
  selector:
    app: {{ include "circleci.fullname" . }}
    #tier: backenddb
    #release: {{ .Release.Name }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "circleci.fullname" . }}-appserver
  labels:
    app: {{ include "circleci.fullname" . }}
    #tier: middletier
    #release: {{ .Release.Name }}
    #chart: {{ include "circleci.chart" . }}
spec:
  type: ClusterIP
  ports:
  - port: 4567
  selector:
    app: {{ include "circleci.fullname" . }}
    #tier: middletier
    #release: {{ .Release.Name }}
